apiVersion: apps/v1
kind: Deployment
metadata:
  name: demolabs-backend
  namespace: demolabs
  labels:
    app: demolabs-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: demolabs-backend
  template:
    metadata:
      labels:
        app: demolabs-backend
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: backend
          # Tag here is rewritten by the Jenkinsfile's "Update K8s Manifests" stage
          # on every build (sed match on this exact registry/repo prefix) - Argo CD
          # then syncs whatever tag is committed.
          image: docker.io/vsutardevops/demolabs-backend:9121d31
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: demolabs-backend-config
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: demolabs-db-secret
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: demolabs-db-secret
                  key: POSTGRES_PASSWORD
          securityContext:
            privileged: false
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: false   # Spring Boot writes to /tmp at runtime
          resources:
            requests:
              cpu: 150m
              memory: 384Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            # JVM + Spring context boot can take a while under load;
            # gives it up to 90s (18 x 5s) before liveness probing kicks in.
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            periodSeconds: 5
            failureThreshold: 18
